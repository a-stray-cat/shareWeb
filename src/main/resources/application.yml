spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/shareweb?useUnicode=true&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: a-stray-cat.demo.mapper
server:
  servlet:
    context-path: /api
